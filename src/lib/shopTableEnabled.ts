/** Center display table removed — line shelves + products only. */
export const SHOP_TABLE_ENABLED = false;

/** Place jewelry GLBs on wall shelves (disabled — shelves only for now). */
export const SHOP_SHELF_PRODUCTS_ENABLED = false;

/** Show 3D shelf.glb units aligned to the boutique background. */
export const SHOP_SHELVES_ENABLED = false;

/** Minimal line-type display shelves (3 per side). */
export const SHOP_LINE_SHELVES_ENABLED = false;

/** Jewelry GLBs on line shelves — one product per shelf line. */
export const SHOP_LINE_SHELF_PRODUCTS_ENABLED = true;
